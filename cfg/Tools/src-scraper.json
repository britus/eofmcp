{
    "name": "sources",
    "title": "EoF MySourceScraper",
    "description": "Source file scraper MySourceScraper",
    "execHandler": "MySourceScraper",
    "execMethod": "sourceScraper",
    "inputSchema": {
        "type": "object",
        "properties": {
            "url": {
                "type": "string",
                "description": "url"
            },
            "operation": {
                "type": "string",
                "description": "string",
                "enum": ["rewiew", "update", "find", "analyse", "show"]
            }
        },
        "required": ["url", "operation"]
    },
    "outputSchema": {
        "type": "object",
        "description": "object",
        "properties": {
            "result": {
                "type": "string",
                "description": "string"
            },
            "success": {
                "type": "boolean",
                "description": "boolean"
            },
            "timestamp": {
                "type": "string",
                "description": "string"
            }
        },
        "required": ["result", "success", "timestamp"]
    },
    "annotations": {
        "audience": ["user", "assistant"],
        "priority": 0.9,
        "lastModified": "2025-01-12T15:00:58Z"
    }
}
